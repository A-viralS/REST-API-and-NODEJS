const math=require('./math.js'); // to import modules here we use require like we used import in react
// or const {add,sub} = require('./math.js')
console.log('Hello World!');
console.log(math);
 console.log(math.sub(1, 2));
